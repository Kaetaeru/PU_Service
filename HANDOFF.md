# 인수인계 — 새 세션 시작 지침

> 작성일: 2026-09-04
> 대상: 이 레포에서 처음 작업을 시작하는 에이전트
> 브랜치: `docs/whatsapp-only`

---

## 0. 먼저 알아야 할 것

이 레포는 **아직 코드가 한 줄도 없다.** 전부 기획 문서다. 커밋 44개가 전부 문서 작업이었다.

방금(2026-09-04) 제품 방향이 크게 바뀌었고, 그 결과가 이 브랜치에 들어 있다. **이전 커밋의 문서를 근거로 판단하면 틀린다.** 아래 §5를 반드시 읽어라.

---

## 1. 프로젝트 한 줄 요약

**StayOps** — 숙소 호스트가 손님과 픽업 기사 사이에서 정보를 복사·재작성해 전달하는 **수동 중계자 역할을 없애는 것**이 유일한 목표다.

레포 이름은 `PU_Service`지만 제품명은 StayOps다.

첫 제품은 공항/역/거점 ↔ 숙소 픽업·드랍오프. V1은 판매용 SaaS가 아니라 **본인들이 실제 숙소 운영에 매일 쓰는 내부 도구**다.

실제 운영 규모: 월 19~20건 수준 (2026년 7~8월 실적 기준).

---

## 2. 정상 동작 흐름

이걸 머리에 넣고 시작해라. 모든 설계가 여기서 나온다.

```text
1. 손님이 웹 폼 작성 → 제출
2. 서버 저장 → 손님에게 "접수됨" WhatsApp
                → 호스트에게 "새 요청" WhatsApp
3. 호스트가 지불 확인 → [배차 요청] 버튼 1회 클릭
4. 대표기사에게 WhatsApp 템플릿 자동 발송 (요약 + 링크)
5. 대표기사가 링크에서 입력:
   배차 가능/불가 · 실제 기사 전화번호 · 차량번호
6. 서버 확정 → 손님에게 배차 안내 WhatsApp
              → 호스트에게 배차 완료 WhatsApp
7. 호스트가 운행 완료 처리
```

**정상 건에서 호스트의 조작은 3번의 버튼 클릭 한 번뿐이다.** 메시지 작성도, 차량번호 옮겨 적기도, 손님 안내 재작성도 없다. 이 불변조건이 깨지는 설계는 전부 틀린 설계다.

---

## 3. 읽는 순서

충돌하면 위가 이긴다.

1. `기술적기획/00_CANONICAL_SYSTEM.md` — 제품 동작, 불변조건, 도메인/상태 모델
2. `기술적기획/V1_INTERNAL_MVP.md` — V1 포함/제외 범위, release gate
3. `기술적기획/06_IMPLEMENTATION_READINESS.md` — 구현 계약, 보안/동시성/테스트/스택
4. `기술적기획/07_WhatsApp_배차_왕복_설계.md` — 배차 왕복 상세
5. `기술적기획/08_요금표.md` — 요금 단일 기준
6. `기술적기획/09_UI_테마.md` — 화면 디자인 기준
7. `기술적기획/05_DEVELOPMENT_PLAN.md` — 단계별 구현 순서
8. `기술적기획/PHASE0_VALIDATION_STATUS.md` — 외부 연동 검증 실제 진행 상황

`.chatgpt-rerun/` 4개 파일은 ChatGPT Rerun 에이전트와의 조정 인터페이스다. 그쪽 프로토콜로 작업할 때만 `README.md` → `control.json` → `STATE.md` → `PLAN.md` 순으로 읽는다. **`control.json`의 `run_id`/`sequence`는 보존해야 한다.**

---

## 4. 2026-09-04에 바뀐 것

| 항목 | 이전 | 현재 |
|---|---|---|
| 메시지 채널 | WhatsApp + KakaoTalk | **WhatsApp만** |
| 기사 쪽 상대 | 개별 기사 여러 명 | **운송회사 대표기사 1명** |
| 배차 발송 | 호스트가 카카오 Share 수동 클릭 | **자동 발송** |
| 호스트 역할 | 승인자 | **관찰자 + 지불 확인 1회** |
| 지불 모델 | `payment_type` 4종 + 분담 계산 | **`payment_confirmed` 불리언 하나** |
| 세금 | 계산 안 함 | **계산함 (공급가 × 10%)** |
| 손님이 보는 금액 | 실시간 견적 표시 | **아무것도 안 보임** |
| 거점 | ICN/GMP/서울역 | **+ 일산 / 용인 에버랜드** |
| 배차 후 차량 변경 | 재확인 흐름 설계 | **시스템으로 처리 안 함 (수동)** |
| UI 기준 | `pickup_2.html` | **`09_UI_테마.md`** |

---

## 5. 틀리기 쉬운 지점 — 반드시 확인

레포에 옛 문서가 그대로 남아 있다. 폐기 표시는 각 문서 맨 위에 붙여뒀지만, 검색으로 코드 조각만 주워 쓰면 틀린다.

### 5.1 폐기된 문서

- **`기술적기획/04_카카오_기사배차_왕복_설계.md`** — 문서 전체가 폐기. `07`이 대체한다
- **`기술적기획/MVP_SPEC.md`** — `guest_paid` / `host_amount` / `payment_type` / `payment_status` / `dispatch_status = waiting|assigned|notified` / §4.2 요금표 / 실시간 요금 표시 전부 폐기
- **`기술적기획/REQUIREMENTS.md`** — 장기 구상 문서. 카카오 알림톡, 정산 자동화 등은 V1 범위 밖
- **`기술적기획/02`, `03`, `docs/pickup-integration-contract.md`** — 독립 HTTP API 전제. 현재는 Supabase Edge Functions

### 5.2 `pickup_2.html`을 그대로 옮기지 마라

입력 항목과 문구는 참고하되, 다음은 **버그이거나 폐기된 값**이다.

- 요금 규칙이 없으면 `return 0` → **0원 금지. 오류를 내야 한다**
- 인원이 구간 밖이면 최상위 구간 자동 적용 → **8명 이상은 거부해야 한다**
- 내장 요금표가 `08_요금표.md`와 다르다 → **08이 기준**
- 방향 값이 `"drop"`이다 → 계약은 `"dropoff"`
- 손님에게 요금을 보여준다 → **보여주면 안 된다**
- 손님 전화번호·언어·수신동의·대형수하물·터미널 항목이 없다 → 전부 신규 추가 필요
- 시각 디자인은 `09_UI_테마.md`가 기준이지 이 파일이 아니다

### 5.3 손님에게 금액을 절대 노출하지 마라

이건 실수하기 쉬우니 따로 적는다.

- 신청 폼에 요금 박스 없음
- 옵션을 켜고 꺼도 금액 변동이 안 보임
- 접수 확인·배차 안내 메시지에 금액 없음
- **API 응답 payload에도 금액 필드를 담지 않는다**

요금은 **호스트와 대표기사에게만** 쓰이는 내부 지표다.

### 5.4 세금은 계산한다 (이전 결론이 뒤집혔다)

`06_IMPLEMENTATION_READINESS.md`의 Major B 본문에 "세금을 추가하지 않는다"는 옛 결론이 남아 있다. **그 항목 맨 위에 뒤집힘 표시가 붙어 있으니 반드시 확인해라.**

```text
공급가 = 기본요금 + 옵션요금      ← 08_요금표.md의 표기 금액
세금   = ROUND(공급가 × 0.1)
총액   = 공급가 + 세금
```

호스트 화면과 기사 메시지에는 **세금포함 총액**을 보여준다.

원래 세금을 뺐던 이유는 "손님이 보던 금액이 10% 올라 보인다"였는데, 손님이 금액을 아예 안 보게 되면서 전제가 사라졌다. 이 구조는 실제 운영 장부(`Stay_Pachira_픽업운영장부_2.xlsx`)와 일치한다.

### 5.5 대표기사는 한 명이다

- 개별 운행 기사 마스터, 기사 계정, 기사 로그인 **없다**
- 여러 기사에게 동시 발송(fan-out), 선착순 경쟁 **없다**
- 무응답 시 자동 다음 기사 이관 **없다** — 호스트에게 알리고 끝
- 실제 운행 기사 정보는 대표기사가 응답 폼에 입력한 값(`driver_phone_e164`, `vehicle_plate`)으로만 존재한다

동시 수락 경쟁은 없지만 **중복 제출 방지 트랜잭션은 유지한다.**

### 5.6 만들지 말아야 할 것

- 배차 확정 후 차량·기사 변경 흐름 (이례적 상황 → 사람이 수동 처리)
- 당일 투어 상품 (요금은 `08_요금표.md` §6.3에 기록만)
- 정산, 입금상태, 지불 분담 계산
- 기사 자유 텍스트 파싱
- 손님 AI 자동 응답
- iCal, 청소, 세탁, 멀티호스트 온보딩

### 5.7 기사 전화번호는 국가번호를 포함한다

손님이 해외 로밍 단말에서 직접 걸 수 있어야 한다. 입력 UI는 국가번호 + 현지번호를 받되 저장 전 E.164로 정규화한다. `libphonenumber-js`를 쓴다.

---

## 6. 열려 있는 항목

### 6.1 요금 — 한 칸 비어 있음

`일산 ↔ 홍대`, `용인 에버랜드 ↔ 홍대` 요금이 없다. **현재는 신청 폼에서 이 조합을 못 고르게 막아둔 상태다.** 가격이 나오면 `08_요금표.md` §2 표에 줄을 추가한다.

나머지 16개 조합은 전부 확정이다.

### 6.2 Phase 0 외부 검증

WhatsApp은 **Meta 테스트 번호로만** 검증됐다. 실제 숙소 운영 번호는 아직 손도 안 댔다.

```text
[PASS] Meta Test WABA / Test Number
[PASS] Test Number → 실단말 outbound
[PASS] Supabase Edge Function webhook verification
[PASS] Test WABA subscription
[PASS] 실단말 → Meta → Supabase inbound POST 200
[PASS] inbound payload parse

[TODO] production raw webhook logging 제거      ← 실손님 데이터 받기 전 필수
[TODO] 실제 운영번호 Coexistence
[TODO] Guest 템플릿 승인
[TODO] 대표기사 템플릿 승인
[TODO] 대표기사 연락처 / WhatsApp 사용 가능 여부 / opt-in
[TODO] 대표기사 실단말에서 URL 버튼 → 응답 웹 오픈
[TODO] Guest/Partner inbound 역할 라우팅 검증
[TODO] opt-in/privacy 문구
```

**절대 하지 말 것:** 현재 운영 중인 WhatsApp Business App 계정 삭제, 번호 연결 해제, API-only 모드로 실수 마이그레이션. `PHASE0_VALIDATION_STATUS.md` §4에 경고가 있다.

### 6.3 가장 큰 리스크 두 개

1. **대표기사가 WhatsApp을 안 쓸 수 있다.** 기술 문제가 아니라 사업 확인 사항이고, 배차 쪽 Phase 0의 첫 액션이다. 안 되면 `PartnerNotificationAdapter`를 `sms`로 바꾸면 되고 도메인은 그대로다
2. **Meta 템플릿 승인이 손님·기사 양쪽 크리티컬 패스에 있다.** 카카오 fallback이 없어졌기 때문. `sms` / `manual` 어댑터가 완충 장치다

---

## 7. 다음에 할 일

### 지금 바로 가능

**손님 신청 폼.** 요금표가 확정됐고 보여줄 항목도 정해져서 막힌 게 없다. `09_UI_테마.md`에 색·서체·컴포넌트 규격이 전부 있다.

### 그다음 (구현 순서)

`06_IMPLEMENTATION_READINESS.md` §15 순서를 따른다.

```text
1. 브랜치 work/v1-internal
2. Vite + React + TypeScript 스켈레톤
3. Supabase 프로젝트/마이그레이션 스켈레톤
4. canonical 스키마 + RLS
5. 요금/검증 로직 + 테스트
6. create_transfer_request 트랜잭션
7. 손님 폼 연결
8. fake/test Notification 어댑터
9. 배차 요청 + signed 응답 트랜잭션
10. 실제 WhatsApp 어댑터 연결
11. 호스트 운영 화면
12. 실기기 E2E
13. 내부 파일럿
```

**중요:** WhatsApp 승인을 기다리는 동안에도 2~9는 전부 구현 가능하다. 공급자 독립적으로 설계돼 있으니 fake 어댑터로 진행해라. 다만 **실손님 파일럿은 시작하지 않는다.**

### 스택 (확정)

```text
Frontend  Vite + React + TypeScript → Cloudflare Pages
Backend   Supabase Postgres / Auth / Edge Functions / RLS
Worker    Supabase Cron (Notification 재시도)
Messaging WhatsApp Cloud API — 단일 WABA 번호, inbound 역할 라우팅
Test      Vitest, 릴리즈 직전 실기기 E2E
의존성    react, react-dom, @supabase/supabase-js, libphonenumber-js
```

UI 프레임워크·컴포넌트 라이브러리는 추가하지 않는다.

---

## 8. 파일 위치

### 레포

```text
github.com/Kaetaeru/PU_Service
브랜치: docs/whatsapp-only
```

`main`에는 이번 개편이 없다. **반드시 `docs/whatsapp-only`를 체크아웃해라.**

### 디자인 참고 원본 (선택)

`09_UI_테마.md`에 색·서체·컴포넌트 CSS가 전부 옮겨져 있어서 **원본 없이도 화면을 만들 수 있다.**

원본을 보고 싶으면 이전 작업 PC의 아래 경로에 있다. 레포에는 넣지 않았다.

```text
OneDrive/Documents/카카오톡 받은 파일/ems-request.html   손님 요청 폼 데모
OneDrive/Documents/카카오톡 받은 파일/ems-admin.html     호스트 확인 화면 데모
```

EMS 배송대행 서비스용이라 내용은 무관하지만, 화면 구조(손님 폼 + 호스트 화면)가 StayOps에 필요한 것과 같아서 디자인 기준으로 삼았다.

### 레포 안 참고 자료

```text
기술적기획/pickup_2.html                     현행 픽업 폼 (§5.2 주의사항 참고)
기술적기획/Stay_Pachira_픽업운영장부_2.xlsx   실제 운영 장부. 요금·정산 구조 근거
```

장부의 `예약장부` 시트에 2026년 7~8월 실제 19건이 있다. 요금 계산 로직 검증용 테스트 케이스로 쓸 수 있다.

---

## 9. 사용자와 일하는 방식

- **한국어로 대화한다.**
- **설명은 실제로 벌어지는 일 중심으로 한다.** 상태 머신, 스키마 필드, 트랜잭션 경계 같은 기술 용어를 나열하는 것보다 "손님이 보내기를 누르면 어디로 뭐가 가고 누가 뭘 본다" 식으로 설명하는 걸 선호한다. 문서 자체는 기술적이어도 되지만 대화는 그렇지 않게.
- 결정이 빠르다. 선택지를 늘어놓는 것보다 권고안을 주고 물어보는 게 낫다.
- 이례적인 상황은 시스템으로 안 만들고 사람이 처리하는 쪽을 선호한다.

---

## 10. 상태 요약

```text
Engineering-ready   YES   — 코드 작성 시작 가능
Phase 0 complete    NO    — 실제 운영번호 미검증
Pilot-ready         NO    — 실손님 받으면 안 됨
구현 시작           NO    — 코드 0줄
```
